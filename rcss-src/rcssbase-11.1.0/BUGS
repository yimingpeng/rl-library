The libloader test does not work on windows in the debug configuration.  I can't figure out why, but the exectable reports that there is "DAMAGE".  For this reason only the release configuration builds and runs loadertest.
