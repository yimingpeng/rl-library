<project name="RandomAgent" basedir="." >

    <property name="src.dir"     value="src"/>

    <property name="build.dir"   value="build"/>
    <property name="dist.dir"   value="../../dist/agentJars"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
	<property name="RLVizLib.jar" value="../../libs/RLVizLib.jar" />
	<property name="jar.result" value="${dist.dir}/${ant.project.name}.jar" />
	
	<path id="application" location="${jar.result}"/>
	<path id="classpath" location="${RLVizLib.jar}"/>
 	

    <target name="clean">
 		<delete dir="${build.dir}"/>
		<delete file="${jar.result}"/>
    </target>

    <target name="compile">
       <mkdir dir="${classes.dir}"/>
        <javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath" /> 
    </target>

    <target name="jar" depends="compile" >
        <mkdir dir="${dist.dir}" />
        <jar destfile="${jar.result}" basedir="${classes.dir}" >
            <manifest>
            </manifest>
        </jar>
    </target>

   <target name="clean-build" depends="clean,jar"/>

<target name="junit" depends="jar">
     <junit printsummary="yes"  >
         <classpath>
              <path refid="application"/>
          </classpath>

         <batchtest fork="yes">
             <fileset dir="${src.dir}" includes="**/*Test.java"/>
         </batchtest>
     </junit>
 </target>


</project>

